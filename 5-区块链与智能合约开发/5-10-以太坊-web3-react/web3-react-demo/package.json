{
  "name": "web3-react-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:contract": "forge test",
    "build:contract": "forge build",
    "deploy:local": "dotenv -- forge script script/DeploySimpleStorage.s.sol:DeploySimpleStorage --rpc-url $RPC_URL --broadcast --sender $DEPLOYER_ADDRESS --unlocked",
    "client:install": "npm install --prefix client --ignore-scripts",
    "client:start": "npm start --prefix client",
    "demo": "dotenv -- node client/web3TestDemo/01-eth相关.js"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2"
  },
  "dependencies": {
    "web3": "4.16.0"
  }
}
