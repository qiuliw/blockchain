{
  "name": "truffle-ipfs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "node node_modules/truffle/build/cli.bundled.js compile",
    "test": "node node_modules/truffle/build/cli.bundled.js test",
    "client:install": "npm install --prefix client --ignore-scripts",
    "client:start": "npm start --prefix client",
    "ipfs:test": "node client/01-ipfs-api-test.js"
  },
  "devDependencies": {
    "truffle": "5.11.5"
  }
}
